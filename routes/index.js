const users = require("./users");
const usersBackend = require("./usersBackend");
const jwt = require("./jwt");
const merchant = require("./merchant");

module.exports = {
  users,
  usersBackend,
  jwt,
  merchant,
};
